import React from 'react'
import PropTypes from 'prop-types'

/**
 * functional react component for ...
 * @function
 * @param {*} props - props
 * @returns {JSX.Element} - react component
 * @useIn - ./
 */
function FileName({ ex }) {
  return (
    <div>
      
    </div>
  )
}

FileName.propTypes = {
 ex: PropTypes.func.isRequired
}

export default FileName


